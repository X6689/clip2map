# Primary persistence. The anon key is public by design; never use service-role.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Temporary Day 3 endpoint retained as the secondary delivery path.
NEXT_PUBLIC_FEEDBACK_ENDPOINT=
NEXT_PUBLIC_FEEDBACK_API_KEY=
